model Product {
  id          String   @id @default(uuid())
  name        String
  description String?
  price       Float
  stock       Int      @default(0)
  image       String?  
  status      String?  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}