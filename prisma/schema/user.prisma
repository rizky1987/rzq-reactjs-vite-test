model User {
  id       String @id @default(uuid()) // UUID String
  name     String
  email    String @unique
  password String
  role     Role   @default(USER)
}

enum Role {
  SUPERADMIN
  ADMIN
  USER
}